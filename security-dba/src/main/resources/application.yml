spring:
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost/securedb
    username: root
    password: rootpass
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true